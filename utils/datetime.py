# coding: utf-8

class datetimeEx:
    @staticmethod
    def now():
        return 0#datetimeEx.totimestamp(datetime.now())


    @staticmethod
    def totimestamp(dt):
        #epoch = datetime(1970, 1, 1)
        #td = dt - epoch
        return 0#(td.microseconds + (td.seconds + td.days * 86400) * 10**6) / 10**6
